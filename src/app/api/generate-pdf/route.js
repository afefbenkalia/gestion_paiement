// app/api/generate-pdf/route.js
import { NextResponse } from 'next/server';

export async function POST(request) {
  // Use a server-side PDF generation service or library
  // that doesn't have compatibility issues
}